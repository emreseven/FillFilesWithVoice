# Core dependencies
streamlit>=1.28.0
openai>=1.0.0
python-docx>=0.8.11

# Optional dependency for microphone recording
# Note: streamlit-mic-recorder may have installation issues on some systems
# If it fails to install, the app will still work but mic recording will be disabled
streamlit-mic-recorder>=0.1.0

# Local session management - no external dependencies needed

# Additional useful dependencies for production deployment
watchdog>=3.0.0  # For streamlit file watching
